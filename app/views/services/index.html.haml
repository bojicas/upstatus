%h1.up_status
  UP Status
  %span.title
    Services List

- @services.each do |service|
  %div.content
    %ul
      %li
        =service.title
        =link_to "Edit", edit_service_path(service), :title => "Edit #{service.title}"
        =link_to 'Destroy', service, :confirm => 'Are you sure?', :method => :delete, :title => "Destroy #{service.title}"
        %ul.details
          %li
            =service.description

%div.link_bar
  =link_to "New Service", new_service_path
